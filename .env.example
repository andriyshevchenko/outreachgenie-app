# OutreachGenie Environment Configuration
# 
# =============================================================================
# IMPORTANT: Development Only
# =============================================================================
# This .env file is for LOCAL DEVELOPMENT AND TESTING ONLY.
# 
# In production, all user secrets (API keys, credentials) are stored in the
# database and encrypted via OS-level security:
# - Windows: DPAPI (Data Protection API)
# - macOS: Keychain API
# 
# Users enter their API keys through the Settings UI, which stores them
# securely in the database. They are NEVER stored in plain text files.
# =============================================================================

# =============================================================================
# LLM Provider (OpenAI) - DEVELOPMENT ONLY
# =============================================================================
# Get your API key from: https://platform.openai.com/api-keys
# Used for: Agent decision-making, chat responses, content generation
# PRODUCTION: Users enter this in Settings UI → stored encrypted in database
OPENAI_API_KEY=sk-your_openai_api_key_here

# =============================================================================
# Web Search (Exa AI) - DEVELOPMENT ONLY
# =============================================================================
# Get your API key from: https://exa.ai/
# Used for: AI-powered web search, company research, prospect discovery
# PRODUCTION: Users enter this in Settings UI → stored encrypted in database
EXA_API_KEY=your_exa_api_key_here

# =============================================================================
# Agent Background Service Configuration
# =============================================================================
# How often (in milliseconds) the agent checks for active campaigns
POLLING_INTERVAL_MS=60000

# Maximum number of campaigns processed simultaneously
# Recommended: 1 (avoid LinkedIn rate limits)
MAX_CONCURRENT_CAMPAIGNS=1

# =============================================================================
# LinkedIn Session Security
# =============================================================================
# LinkedIn cookies are stored in database, encrypted via:
# - Windows: DPAPI (Data Protection API) - automatic, tied to user account
# - macOS: Keychain API - secure credential storage
# No configuration needed - OS handles encryption automatically

# =============================================================================
# User Secrets Storage (Production)
# =============================================================================
# All user-provided credentials are stored in the database:
# - OpenAI API keys
# - Exa API keys  
# - LinkedIn session cookies
# - Any other sensitive data
#
# Encryption:
# - Windows: DPAPI encrypts data using user's Windows credentials
# - macOS: Keychain API provides secure storage
# - Keys cannot be decrypted by other users on the same machine
# - No plaintext secrets in files or database
#
# Users manage secrets through the Settings UI page.

# =============================================================================
# Database Configuration
# =============================================================================
# SQLite database path (local-first storage)
DATABASE_PATH=./outreachgenie.db

# =============================================================================
# Logging Configuration
# =============================================================================
# Log level: Trace | Debug | Information | Warning | Error | Critical
LOG_LEVEL=Information

# Log file path
LOG_PATH=~/Documents/OutreachGenie/logs

# =============================================================================
# Rate Limiting (Post-MVP)
# =============================================================================
# Maximum chat messages per minute
CHAT_RATE_LIMIT=10

# Maximum concurrent Exa searches
EXA_CONCURRENCY_LIMIT=3

# =============================================================================
# Security (Post-MVP)
# =============================================================================
# Enable secure credential storage (Windows Credential Manager / macOS Keychain)
USE_SECURE_STORAGE=false

# =============================================================================
# .NET Aspire (Post-MVP)
# =============================================================================
# Enable Aspire orchestration dashboard
ENABLE_ASPIRE=false

# =============================================================================
# Notes
# =============================================================================
# - Never commit .env file to version control
# - API keys are user-specific (each user provides their own)
# - This app runs locally on user machines, not cloud-deployed
# - See README.md for setup instructions
